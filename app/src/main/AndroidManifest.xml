<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.l.secretfriend">

    <application

        android:allowBackup="true"
        android:icon="@drawable/ic_secretfriend"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Secretfriend"
       >
        <activity
            android:name=".Controller.Controller.TGroup"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Controller.Controller.TList"/>
    </application>

</manifest>